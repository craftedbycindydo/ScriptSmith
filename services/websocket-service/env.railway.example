# WebSocket Service Environment Configuration for Railway.app
# Copy these variables to your Railway project environment settings

# Backend API URL (should point to your deployed backend service)
# Example: https://your-backend-service.railway.app
BACKEND_URL=https://${{backend.RAILWAY_PUBLIC_DOMAIN}}

# Frontend origins for CORS (comma-separated list)
# Example: https://your-frontend.railway.app,https://your-custom-domain.com
CORS_ORIGINS=https://${{frontend.RAILWAY_PUBLIC_DOMAIN}}

# Optional: Custom port (Railway will auto-assign if not specified)
# PORT=8007

# Node.js Environment
NODE_ENV=production

# Optional: Socket.IO configuration for production optimization
# SOCKET_PING_TIMEOUT=60000        # How long to wait for ping response (default: 60s)
# SOCKET_PING_INTERVAL=25000       # How often to send pings (default: 25s)

# Optional: Session cleanup interval (in milliseconds)
# SESSION_CLEANUP_INTERVAL=3600000 # How often to clean up inactive sessions (default: 1 hour)
