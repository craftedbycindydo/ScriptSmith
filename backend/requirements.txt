fastapi==0.116.1
uvicorn==0.35.0
sqlalchemy==2.0.43
psycopg2-binary==2.9.10
redis==6.4.0
python-multipart==0.0.20
pydantic==2.12.5  # floor set by mcp 2.0
pydantic-settings==2.7.0
alembic==1.16.4
python-dotenv==1.1.1

# Security & Authentication
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
argon2-cffi==25.1.0
email-validator==2.2.0

# HTTP client for microservices (replaces Docker)
aiohttp==3.9.1
httpx==0.27.0

# WebSocket is now handled by separate Node.js service

# File operations
aiofiles==24.1.0
certifi

# Spreadsheet export (gradebook .xlsx)
openpyxl==3.1.2

# MCP connector (app/mcp). Pulls anyio>=4.9, httpx2 and pyjwt; httpx and
# uvicorn are unaffected and stay where they are.
mcp==2.0.0
